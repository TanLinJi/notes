- Instance Accuracy: 样本级整体准确率（微平均），统计所有样本的正确比例。
- Class Accuracy: 类别级平均准确率（宏平均），先算每个类别的准确率，再对类别求平均。

- Instance Accuracy: 代码在每个批次上计算准确率后再取平均，若各批次大小一致，它近似等同于标准微平均；若最后一批更小或批次大小不一，会有轻微偏差。
- Class Accuracy: 代码对每个类别在各批次中的准确率先平均，再对类别平均；本质是宏平均的近似版本，批次层面的加权可能导致与严格宏平均略有差异。

区别：

- 侧重点不同: Instance Accuracy更看总体样本，容易被“大类”主导；Class Accuracy对每个类别一视同仁，能更好反映“小类”的表现。
- 对不均衡敏感性: 类别分布不均衡时，Instance Accuracy通常更高；Class Accuracy会更严格，数值往往更低。



### 超参数说明

#### 1. 模型与环境配置

- **model='pointnet2_part_seg_msg'**: 指定使用的**网络模型名称**。
  - `part_seg`: 代表零件分割（Part Segmentation）任务。
  - `msg`: 代表 Multi-Scale Grouping。这意味着模型会使用不同半径的采样区域来提取点云特征，对不同大小的形状更鲁棒。
- **gpu='0'**: 指定使用的 **GPU 编号**。如果你有多个显卡，`'0'` 表示使用第一张显卡。
- **log_dir='pointnet2_part_seg_msg'**: **日志存放目录**。训练过程中的模型权重（`.pth`文件）、损失曲线和准确率记录都会保存在这个文件夹里。

#### 2. 训练流程控制

- **batch_size=16**: **批次大小**。模型一次性读入 16 个点云模型进行计算。这个值受限于显存大小，显存越大可以设得越高。
- **epoch=251**: **总训练轮次**。整个训练数据集将被模型完整地“学习” 251 遍。
- **optimizer='Adam'**: **优化器类型**。Adam 是一种常用的自动调整学习率的优化算法，通常比普通的 SGD（随机梯度下降）收敛更快。

#### 3. 学习率策略（决定收敛速度）

- **learning_rate=0.001**: **初始学习率**。控制参数更新的步长。太大可能导致无法收敛，太小则训练太慢。
- **decay_rate=0.0001**: **权重衰减（L2 正则化）**。为了防止模型过拟合，对较大的权重进行惩罚。
- **step_size=20** & **lr_decay=0.5**: 这一组决定了**学习率如何下降**。
  - 意思是：每训练 **20** 个 epoch（`step_size`），学习率就乘以 **0.5**（`lr_decay`，学习率衰减率）。
  - 例如：第 1-20 轮是 0.001，第 21-40 轮就会降到 0.0005。这能帮助模型在后期更稳定地寻找最优解。

#### 4. 点云特定参数

- **npoint=2048**: **采样点数量**。从原始点云中随机或按照特定算法挑选出 2048 个点输入网络。点数越多，细节越丰富，但计算量越大。
- **normal=True**: **是否使用法向量**。
  - 如果为 `True`，输入数据除了坐标 $(x, y, z)$，还会包含每个点的法线方向 $(nx, ny, nz)$，总共 6 个通道。这对于识别物体的表面特征（如平整度、边缘）非常有帮助。